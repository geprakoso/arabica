{
  "name": "purchase-module-e2e-tests",
  "version": "1.0.0",
  "description": "E2E Tests for Purchase Module Policy using Puppeteer",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:headless": "jest --headless=false"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "puppeteer": "^21.11.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/*.e2e.test.js"],
    "setupFilesAfterEnv": ["./setup.js"]
  }
}